import React from 'react';
import PropTypes from 'prop-types';

const NewsPage = () => (
  <div>
    <h2 style={{marginTop: 200, marginLeft: 50}}>Hydro House NewsPage</h2>
  </div>
);

NewsPage.propTypes = {
  testId: PropTypes.string,
};

export default NewsPage;
