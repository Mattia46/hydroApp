import React from 'react';
import PropTypes from 'prop-types';

const OrdersPage = () => (
  <div>
    <h2 style={{marginTop: 200, marginLeft: 50}}>Hydro House OrdersPage</h2>
  </div>
);

OrdersPage.propTypes = {
  testId: PropTypes.string,
};

export default OrdersPage;
